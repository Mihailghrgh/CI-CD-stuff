name: Workflow-Event

on: 
    workflow_dispatch: 

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: Show the trigger
        run: echo "Triggered by a(n) ${{github.event_name}} event."
